[project]
name = "hexlet-code"
version = "0.1.0"
description = "Booking calls service (Hexlet AI for developers project)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
